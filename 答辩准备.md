# 答辩准备（2024国赛B题 · 全题）

> 更新：2026-08-16（依据 paper/main.tex 论文源文件重写"做法"部分）
> 用途：答辩陈述 + 高频问题预案 + 核心数字速查

## 一、开场（30秒定调）

> "我们用一套统一的统计-决策框架解决了四问：**精确概率计算保证可信，全枚举保证全局最优，蒙特卡洛保证可验证，贝叶斯 EVSI 把抽样做成了决策变量**。四个问题的递进关系是：问题1 给统计判定基础 → 问题2/3 在已知参数下优化生产决策 → 问题4 反过来回答'抽样在经济上值不值得'。"

## 二、PPT 结构（10~12 分钟）

| 页 | 内容 | 时间 | 必讲要点 |
|:---|:---|:---:|:---|
| 1 | 题目与总路线图 | 1min | 四问递进：统计判定→单级决策→多级推广→信息价值闭环 |
| 2 | 问题1 | 2min | 只讲通式 n=⌈ln(1−信度)/ln(1−p₀)⌉；精确二项、最小性可证明 |
| 3 | 问题2 | 2.5min | 口径统一 + q 质量传播 + 16 枚举 + 6 情形表 + MC 验证 |
| 4 | 问题3 | 2.5min | 级联递推=动态规划自底向上；66.09 元；翻转检验图（zf 亏 51 元） |
| 5 | 问题4 | 2.5min | EVSI 决策链流程图；平台效应（n\*=0）；批量相图（B=10000 时值钱） |
| 6 | 总结 | 1min | 三层验证体系 + "统计回答怎么抽，经济回答值不值得抽" |

---

## 三、每题详细做法（讲稿：用了什么方法、怎么得出答案）

### 问题1：抽样检测方案（统计判定）

**建模思路**：题目给"信度"但没有统计课本定义 → 我们辨析后采用**判定可靠度**读法：信度=方案在临界处（p=p₀）作出判定的可靠性。

**步骤**：
1. 抽检 n 件，次品数 X ~ Bin(n, p)；验收规则"零次品接收、≥1 件拒收"；
2. 情形一（95% 拒收）：约束 1−(1−p₀)ⁿ ≥ 0.95；情形二（90% 接收）：约束 (1−p₀)ⁿ ≤ 0.10；
3. 两条约束解得同一个通式 n = ⌈ln α/ln(1−p₀)⌉——代入得 **n₁=29、n₂=22**；
4. **最小性证明**：n−1 不满足约束（28 件时可靠度 94.77%<95%），故全局最小；
5. **检验**：① 闭式公式与精确搜索（1~100）完全一致；② 单调性检验：n 对信度单调不减、对 p₀ 单调不增（0.80~0.99 扫描通过）；③ SPRT 作为附录扩展——在 p 远离 p₀ 时省样本，但难判区反而更费，且需自备备择点，不符合题目"最简"要求。

**一句话**：把模糊的"信度"翻译成精确二项尾概率约束，得到可证明最小的闭式解。

### 问题2：两零配件生产决策（解析期望利润 + 全枚举）

**建模思路**：4 个 0-1 决策（配件1检测 x₁、配件2检测 x₂、成品检测 x₃、拆解 x₄）→ 16 策略。关键创新是**统一核算口径**：题目要求"无条件调换"→ 企业最终必须交付一件合格品 → 以"每服务一名客户"为单位，拆解循环与报废重做两个分支才可比较。

**步骤**：
1. **质量传播**：进入装配的配件次品率 eᵢ=pᵢ(1−xᵢ)；成品实际不合格率 q=1−(1−e₁)(1−e₂)(1−p₀)（题目给的 p₀ 是"正品配件装配后"的工艺次品率，附录1）；
2. **成本三件套**：首轮配件成本 C_part（检测时含重购几何级数 (c+d)/(1−p)）；回收件成本 D（免采购但要再检测）；每轮基础成本 B=a+x₃d₀；
3. **两分支期望成本**：拆解（x₄=1）→ C=C_part+[B+q(f+D)]/(1−q)；不拆解（x₄=0）→ 失败整体重做 C=(C_part+B)/(1−q)。分母 1−q 是循环几何级数的期望轮数；
4. **收入**：x₃=1 时 s；x₃=0 时 s−q·r/(1−q)（退回损失摊入循环）；
5. **求解**：16 策略全枚举（比启发式可靠：无初值依赖、全局最优可复核）→ 6 情形最优表；
6. **验证三层**：① 蒙特卡洛 20 万次逐件模拟，误差 0.04%~1.25%，解析最优==模拟最优；② 单参数分岔（d₁=1.25→1.5 翻转、r=10→12 翻转、f=12→13 翻转——阈值响应符合业务逻辑）；③ **跨情形遗憾分析**：情形未知时策略 0011 平均遗憾仅 1.60、最大遗憾 4.83 元/件，是最稳健统一方案。

**结果规律**：配件2（贵、18元）一律不检测；配件1 只在 20% 次品率情形检测；调换损失大（≥12）才检测成品；拆解费 5 元时拆解、40 元时（情形6）不拆解。

### 问题3：多级装配系统（级联递推 = 自底向上动态规划）

**建模思路**：问题2 的公式不是一次性工具，而是**可嵌套模块**——每个半成品节点把上游零配件当作自己的"广义输入"，聚合质量与成本后成为成品层的"广义零配件"。

**步骤**：
1. 16 个 0-1 决策（8 配件检测 + 3 半成品检测 + 3 半成品拆解 + 成品检测拆解）→ 2¹⁶=65536 策略；
2. **级联质量传播**：eⱼ=pⱼ(1−xⱼ) → qᵢ=1−(1−p_h)∏(1−eⱼ) → q_f=1−(1−p_f)∏[1−qᵢ(1−yᵢ)]。最优策略下 q_f=1−0.9⁴=**34.39%**——单节点 10% 风险经 4 次组合放大的直接证据；
3. **成本递推三层**：配件层 Cⱼ/Dⱼ → 半成品层 Eᵢ/Rᵢ（检测触发"不合格—拆解/报废—重做"循环，首轮成本 ΣCⱼ 与回收件成本 ΣDⱼ 必须区分，否则重复计采购）→ 成品层几何级数同问题2；
4. **求解**：65536 全枚举（NumPy 向量化约 2 秒）→ 最优策略 **11111111|000|000|11**（配件全检+成品检测拆解），利润 66.09 元/件；
5. **等价编码说明**：8 个最优编码仅在 z₁z₂z₃ 不同——半成品不检测时拆解决策不触发，是编码等价类而非多个最优解；
6. **验证**：① MC 误差 0.064%；② 骨架稳定性扫描（售价 80~320 不变；F_F 增大时"成品拆解→半成品检测"迁移）；③ 决策树规则（配件被检率 64%~96%，留出准确率 88%）；④ **单比特翻转检验**：13 降/3 不变/0 升，zf 翻转损失 51.02 元——全局最优+局部稳定双重保证。

**核心洞察**：**下游集中兜底**——半成品逐级检测（4 元×3）比"让次品传到成品统一处理"更贵，质量控制集中在价值最高的下游节点。

### 问题4：抽样-决策一体化（贝叶斯 EVSI）

**建模思路**：题目说"次品率是通过抽样得到的"——侧重点是**方法**而非数字。所以不是"拿抽样值重算"，而是把**抽样本身做成决策变量**：抽多少、看到什么、怎么改决策、信息值多少钱，同一框架回答。

**步骤**：
1. **共轭模型**：pⱼ ~ Beta(a₀ⱼ,b₀ⱼ)（均值=表值，等价样本量 n_prior=30）→ 抽 n 件观测 kⱼ~Bin(n,pⱼ) → 后验 pⱼ|kⱼ ~ Beta(a₀ⱼ+kⱼ, b₀ⱼ+n−kⱼ)；
2. **观测驱动决策**：x*(k,n)=argmax E_{p|k}[π(p,x)]——生产策略是观测结果的**函数**（决策表自动生成，如"看到配件1次品变多→开启配件1检测"）；
3. **事前价值**：V(n)=E_{p,k}[maxₓ E_{p|k}[π]]−3nd/B（抽样成本每批一次摊入批量 B）；
4. **指标**：n*=argmax V(n)，EVSI=V(n*)−V(0)；
5. **计算**：嵌套蒙特卡洛 + 共同随机数（CRN）降方差，300 世界 × 500 后验样本；
6. **结果**：默认参数（B=1000,d=2）下仅情形2 在 n*=10 处 EVSI=0.049 元/件，其余 n*=0——**利润平台效应**：策略标签会翻，但候选策略利润差极小，信息净价值为负；
7. **批量相图**（亮点）：B×d 网格扫描——B=10000 时情形6 EVSI 达 0.65 元/件（每批 6500 元）→ **批量决定抽样值不值**，这是"问题1 抽样方案在经济上是否值得"的答案；
8. **稳定性检验**：10 个随机种子重复——定性结论（情形6>1/2、量级）跨种子稳定；定量 n* 在平台区间波动（这正是平台效应的证据，主动说明）。

**一句话**：问题1 按信度定样本量、问题4 按利润定样本量，目标函数不同——"统计回答怎么抽，经济回答值不值得抽"。

---

## 四、高频问题预案（按命中概率排序）

**Q1：问题1 为什么用 29/22，公开解答是 98/60？**
98/60 是正态近似+两点功效口径，需额外假设备择点 p₁；且 n=29 时 np=2.9<5，**正态近似条件本身不满足**。我们用精确二项分布避免近似失效；"信度"读作判定可靠度，论文 §5.1 写明辨析。

**Q2：问题2 情形1 你们拆解（0001），参考解不拆解，谁对？**
差异在口径——我们严格按"拆解不损坏零配件+重复步骤"建模，拆解费 5 元换回 22 元配件价值；20 万次逐件模拟与解析一致（误差 0.2%），模型内部自洽。这是建模假设差异，不是计算错误。

**Q3：问题4 为什么大部分情形 n\*=0？抽样白做了？**
这正是利润平台效应的结论：候选策略利润差只有几毛钱，抽样成本摊到批量后净价值为负。但批量相图显示 B=10000 时情形6 EVSI 达 0.65 元/件（6500 元/批）——**批量决定抽样值不值**。问题1 看信度（物理基础）、问题4 看价值（经济决策），不矛盾。

**Q4：稳态混合假设是不是太强了？**
承认局限。回收件按节点质量分布重采样，不追踪单件历史；严格建模需马尔可夫链但会失去闭式解。论文已列为基本假设并说明适用条件（大规模流水线）。

**Q5：你们凭什么说枚举的结果是全局最优？**
策略空间 16/65536 是有限的，全枚举=穷尽所有可能，不存在局部最优概念；MC 模拟独立验证了利润值；单比特翻转检验（0 个变好）进一步证明没有遗漏邻域解。

**Q6：遗憾分析是什么？为什么重要？**
情形未知时企业必须预先选一个统一策略，遗憾=该策略在该情形的利润损失。0011 平均遗憾 1.60、最大 4.83 元/件，为 16 策略最小最大遗憾——这是给企业的"保险方案"。

**Q7：你们的创新点是什么？**
① P4 抽样内生化（n 是决策变量、观测驱动决策表、EVSI 量化信息价值）——公开解答普遍只做"重估次品率再重解"；② 全卷精确计算+全枚举，全局最优可证明；③ 三层验证（解析/枚举/蒙特卡洛）+翻转检验+稳定性检验+遗憾分析。

**Q8：决策树和全枚举什么关系？**
决策树（留出准确率 88%）只是 65536 枚举结果的可解释压缩——提炼"检测费≤1.5 元的便宜件更该检测"这类规则给企业用；结论一律以全枚举为准，树不是求解器。

---

## 五、加分策略

1. **主动暴露"小缺陷"再给解释**（n\*=0 的平台效应、n\* 跨种子波动）——预判评委问题；
2. **数字感**：66.0869、0.3439、51.02、1.60/4.83（遗憾）、0.064% 都能报出来源；
3. **方法边界意识**：结尾一句"小规模精确求解、大规模可用 DP/分支定界/消息传递扩展"；
4. **被问住时**："这个点我们考虑过 X，最终选了 Y，理由是 Z"——永远给选择理由。

## 六、注意事项

- 时间严格 12 分钟内；每题用"模型→结果→验证"三句话骨架展开；
- 最强三张图：P3 翻转检验图、P4 批量相图、P2 遗憾分析图——确保现场可见；
- 不念 PPT、不逐行讲公式（评委有论文，讲思路链）；
- 质疑时先承认假设再给理由（"这个假设确实强，我们做了敏感性来缓解"）。

## 七、核心数字速查（与论文一致）

| 项目 | 数字 |
|:---|:---|
| P1 样本量 | 拒收 n=29（P=0.9529）、接收 n=22（P=0.0985，可靠度 0.9015） |
| P1 通式 | n = ⌈ln α/ln(1−p₀)⌉；p₀=1% 时 n₁=299，p₀=20% 时 n₁=14 |
| P2 最优策略 | 1:0001(21.68) 2:1001(12.97) 3:0011(19.80) 4:1011(15.17) 5:0011(18.94) 6:0000(21.68) |
| P2 遗憾分析 | 0011 平均遗憾 1.60、最大遗憾 4.83 元/件（最小最大遗憾策略） |
| P2 验证 | MC 20 万次，误差 0.04%~1.25%；分岔点 d₁=1.25~1.5、r=10~12、f=12~13 |
| P3 最优策略 | 11111111\|000\|000\|11，利润 66.0869，q_f=34.39%，期望轮数 1.52 |
| P3 翻转检验 | 13 降/3 等价/0 升；zf 51.02 > yf 11.82 > y 7.55~8.06 > x 0.51~2.73 |
| P3 验证 | MC 误差 0.064%；决策树留出准确率 88% |
| P4 框架 | Beta(n_prior=30) → Bin(n) → Beta(a₀+k, b₀+n−k)；V(n)=E[π]−3nd/B；CRN |
| P4 默认结果 | 仅情形2 n\*=10、EVSI=0.049；其余 n\*=0（利润平台效应） |
| P4 批量相图 | 情形6：B=1000 时 (n\*,EVSI) 最高 (70,0.37)；B=10000 时 d=0.5→(100,0.41)、d=2→(100,0.49)、d=5→(70,0.65)，约 4100~6500 元/批 |
| P4 稳定性 | 10 种子：情形6 EVSI 0.185±0.149 显著高于情形1(0.044±0.081)/2(0.019±0.036) |
| P4 决策表示例 | 情形2：k=(0,0,0)→0000；(1,1,1)→1001；(2,2,2)→1011；(3,3,3)→1111 |
