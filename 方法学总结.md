# 方法学总结（2024国赛B题 · 全题）

> 生成时间：2026-08-12
> 用途：论文"模型评价"章节素材；建模手/编程手/论文手统一方法学口径
> 关联：`论文写作总纲.md`、`p1~p3/论文写作指南.md`、`p4/论文写作指南_evsi版.md`

---

## 一、问题1：抽样检测方案

**方法**：单侧假设检验（二项分布**精确计算**，无近似）+ "判定可靠度"读法

**做法**：

1. 设 X ~ B(n, p)，判定规则统一为"次品数≥1 拒收，0 件接收"；
2. 情形一（95% 信度拒收）：约束 P(X≥1|p₀) = 1−(1−p₀)ⁿ ≥ 0.95 → **n=29**；
3. 情形二（90% 信度接收）：约束 P(X=0|p₀) = (1−p₀)ⁿ ≤ 0.10 → **n=22**；
4. 通解公式 n = ⌈ln(1−信度)/ln(1−p₀)⌉；精确搜索验证最小性（n−1 必不满足）；
5. 扩展：标称值敏感性表（n 随 p₀ 变化，p1_sensitivity.csv）、SPRT 对比（p1_sprt.csv + fig4）。

| 特点                        | 说明                                                                                                          |
| :-------------------------- | :------------------------------------------------------------------------------------------------------------ |
| ✅ 精确无近似               | 全部用精确二项分布，不用正态近似（公开解答的 98/60 依赖正态近似且需额外假设 p₁；我们样本量更小、假设更透明） |
| ✅ 闭式解                   | 通解公式可套任意标称值                                                                                        |
| ✅ 最小性可证明             | n−1 不满足约束，最小值有严格证明                                                                             |
| ❌ "信度"读法有歧义         | 若按"显著性水平"读法（错误率≤5%）会得到 n=2 的平凡方案；我们选"判定可靠度"读法并在论文中写明辨析             |
| ❌ 假设检测无误差、无限批量 | 未考虑检测设备误判率、小批量超几何分布                                                                        |
| ❌ 只最小化 n 本身          | 未把抽样成本与后续决策价值挂钩（由 P4 优化补上闭环）                                                          |

---

## 二、问题2：两零配件生产决策

**方法**：**期望利润解析模型**（口径B：以"服务一个客户"为单位）+ **16 策略全枚举** + **蒙特卡洛交叉验证**

**做法**：

1. 质量传递：成品实际不合格率 q = 1−(1−e₁)(1−e₂)(1−p₀)（附录1 的必然推论）；
2. 期望成本解析：配件重购几何级数 (c+d)/(1−p)、拆解循环期望轮数 1/(1−q)、报废重做同构公式——两分支口径统一（与题目"调换"要求一致）；
3. 4 个 0-1 决策 × 6 情形全枚举 → 全局最优策略表 + 成本分解（配件/装配/检测/拆解/调换）；
4. 蒙特卡洛 N=20 万逐件模拟验证，误差 <1.3%，解析最优==模拟最优。

| 特点              | 说明                                                                                                 |
| :---------------- | :--------------------------------------------------------------------------------------------------- |
| ✅ 口径严谨       | 每客户单位与"无条件调换"完全一致；修正版（口径B）使情形6 决策符合直觉（拆解费40→不拆解）            |
| ✅ 全局最优       | 16 组合穷举无局部最优陷阱                                                                            |
| ✅ 双重验证       | 解析=模拟，模型内部一致性有实证                                                                      |
| ✅ 成本分解可解释 | 每项期望成本单独给出，支撑决策依据                                                                   |
| ❌ 稳态混合假设   | 回收件质量按总体分布独立重采样；若严格追踪"同一件次品配件"，循环理论上无界——现实中企业会设循环上限 |
| ❌ 检测无误差     | 未建模漏检/误检                                                                                      |
| ❌ 单周期无库存   | 未考虑拆解回收件的跨批次库存缓冲、批量折扣                                                           |
| ❌ 需求无限       | 假设合格品必能按售价 s 卖出                                                                          |

---

## 三、问题3：多级生产决策

**方法**：**自底向上级联递推**（质量传递 × 期望成本）+ **2¹⁶=65536 全枚举** + **蒙特卡洛验证**

**做法**：

1. 质量级联：qᵢ = 1−∏(1−eⱼ)(1−p_h)、q_f = 1−∏(1−e_hᵢ)(1−p_f)；
2. 期望成本递推：配件层 Cⱼ/Dⱼ → 半成品层 Eᵢ/Rᵢ（含拆解循环、回收件再处理）→ 成品层（几何级数）——与问题2 公式逐级同构嵌套；
3. 16 决策全枚举求全局最优（配件全检测+成品检测拆解，**66.09 元/件**）；
4. 蒙特卡洛 N=20 万，误差 <0.65%。

| 特点                 | 说明                                                               |
| :------------------- | :----------------------------------------------------------------- |
| ✅ 体系统一          | 与 P2 同一组公式嵌套推广，论文"模型体系一致性"是最大加分点         |
| ✅ 可推广任意 m 工序 | 递推结构天然支持题目要求的"m 道工序、n 个零配件"推广               |
| ✅ 全局最优+验证     | 65536 穷举 + MC 双重确认                                           |
| ✅ 级联洞察          | q_f=34.39%（三个 10% 叠加），"下游集中兜底"规律直观                |
| ❌ 状态空间指数爆炸  | 变量数 = 2n+m+1，配件一多（如 20 个）枚举不可行，需动态规划/启发式 |
| ❌ 稳态假设被放大    | 两级循环（半成品拆解×成品拆解）嵌套，稳态混合假设作用范围更大     |
| ❌ 拆解决策粒度粗    | 未区分"检测出不合格"与"退回"两种拆解触发场景（可扩展为独立变量）   |

---

## 四、问题4：抽样-决策一体化（EVSI 框架）

**方法**：**贝叶斯决策论 + EVSI（抽样信息价值）**——把"抽样检测方法"本身建进决策链（方法层修正：题目侧重点是方法，不能只做参数敏感性重解）

**做法**：

1. 先验：p ~ Beta(a₀, b₀)，均值=表值 p̂，等价样本量 n_prior=30（反映对供应商的信任）；
2. 抽样：观测 k | p ~ Binomial(n, p)，**n 为决策变量**；
3. 后验：p | k ~ Beta(a₀+k, b₀+n−k)；
4. 观测驱动决策：x\*(k) = argmaxₓ E_{p|k}[π(p,x)]（**决策规则=观测的函数**，决策表自动生成）；
5. 事前价值：V(n) = E_{p,k}[maxₓ E_{p|k}[π]] − 3n·d/B（抽样成本每批一次，摊入批量 B）；
6. 最优抽样量 n\* = argmax V(n)；EVSI = V(n\*) − V(0)；
7. 实现细节：numpy 向量化 + 同一组后验样本配对比较 16 策略（消除策略间采样噪声）+ CRN（Common Random Numbers，同一世界随机数评估所有 n，V(n) 曲线平滑）。

**结果**：默认（B=1000, d=2）多数情形 n\*=0（利润平台效应：候选策略利润差小，EVSI≈0.0~0.05 元/件）；批量敏感性显示 **B=100→n\*=0，B=1000→n\*=10~70，B=10000→n\*=70~100**（情形6，EVSI 0.65 元/件×批量）——**批量决定抽样值不值**；低次品率（5%）信息价值高于 10%；从经济角度回答了"问题1 为什么值得设计抽样方案"。

| 特点                | 说明                                                                                                 |
| :------------------ | :--------------------------------------------------------------------------------------------------- |
| ✅ 方法内化         | 抽样量内生、观测驱动决策，符合题目"通过抽样检测方法得到"的侧重点（修正了"CI 端点重解"=代数字的缺陷） |
| ✅ 决策规则自动生成 | 观测 k → 策略 x\*(k) 的决策表由后验期望利润自动得到，无需人工假设                                   |
| ✅ 信息价值量化     | EVSI 直接给出抽样的经济价值（元/件），与抽样成本比较决定 n\*                                         |
| ✅ 批量敏感性       | 批量 B 是决定抽样值不值的关键参数（题目未给，做 100/1000/10000 敏感性）                              |
| ❌ 先验主观         | Beta 先验的均值/强度需假设（n_prior 敏感性可缓解）                                                   |
| ❌ 批量需假设       | B 是外生参数，论文需说明并做敏感性                                                                   |
| ❌ 计算成本         | 后验期望利润靠蒙特卡洛（M=500），结果带噪声（n\* 在小 EVSI 处对噪声敏感）                            |
| ❌ 三参数独立       | p1/p2/p0 独立抽样观测，未考虑相关性                                                                  |

---

## 五、优化扩展的方法学定位

| 扩展                    | 方法                         | 角色                                                                            |
| :---------------------- | :--------------------------- | :------------------------------------------------------------------------------ |
| 决策分岔图（P2/P3）     | 单参数扫描 + 全枚举          | **验证性**：分岔点与 6 情形决策自洽，反向验证模型正确性                   |
| 决策树规则提炼（P2/P3） | 参数空间采样 + 枚举 + 决策树 | **解释性**：从枚举数据学可解释规则（P3 准确率 89.6%），给企业傻瓜式决策表 |
| SPRT 对比（P1）         | 序贯似然比 + 蒙特卡洛        | **对比性**：证明固定方案在边界处更优，反证我们的方案                      |
| EVSI 批量敏感性（P4）   | 贝叶斯决策论 + CRN 蒙特卡洛  | **升华性**：批量决定抽样值不值，四问经济闭环                              |

---

## 六、横向总体评价

| 维度                 | 我们的做法                                               | 潜在风险                                      |
| :------------------- | :------------------------------------------------------- | :-------------------------------------------- |
| **精确性**     | 全程精确计算/解析，不用近似                              | 稳态混合、检测无误差假设是最大近似点          |
| **最优性**     | 全枚举=全局最优，可证明                                  | 配件数增多时不可行（需 DP）                   |
| **可解释性**   | 解析公式+成本分解+规则树，全链可解释                     | 决策树规则准确率仅 84~90%，非 100% 还原       |
| **稳健性**     | 批量/检测费/先验强度敏感性 + 分岔点 + MC 验证，多角度    | EVSI 依赖先验与批量假设（均做敏感性）         |
| **论文风险点** | P1 信度读法 vs 主流 98/60；P2 情形1 拆解 vs 参考解不拆解 | 已在`论文写作总纲.md`"口径统一清单"写辩护词 |

---

## 七、机器学习在本文中的定位（论文提一嘴素材）

> 决策：不做额外 ML 实现，论文中以文字形式在"模型评价与推广"章节提及，体现方法前瞻性。

**建议段落（可直接粘贴或改写）：**

> 本文在模型求解上采用了精确解析与全枚举方法，保证全局最优与可复现性；同时，为增强决策的可解释性，本文从参数空间采样与全枚举产生的数据中训练了浅层决策树（留出集准确率 84%~90%），提炼出"检测费-次品率-拆解费"的阈值型决策规则（图 X），为企业提供可直接套用的决策表。
>
> 值得指出的是，机器学习在本模型中还有两类扩展空间：其一，以多层感知机等深度模型替代浅层决策树作为"参数→最优策略"的替代模型，可在牺牲可解释性的前提下将拟合精度进一步提升；其二，当生产规模推广至大规模场景（如 m 道工序、n 个零配件，决策变量超过数十个）时，2^n 全枚举不再可行，可将生产决策建模为马尔可夫决策过程（MDP），采用深度强化学习（如 PPO、DQN）训练策略网络求近似最优解。本文的级联递推结构恰好为这类方法提供了天然的 MDP 状态与动作定义，二者可作为未来工作的衔接点。

**注意**：

- 该段落必须出现在"模型评价/推广"小节，**不得**出现在主求解路径中；
- 若篇幅紧张，可只保留第一段（决策树规则提炼，是已实现的成果）与"其二"一句展望；
- 不要在论文中声称"用神经网络求解了本题"——精确方法才是主角。

## 八、论文使用建议

1. 各题"方法-做法-特点-缺点"表格直接作为**第七章 模型评价**的骨架；
2. 五个横向维度（精确性/最优性/可解释性/稳健性/风险点）作为**模型综合评价**小节；
3. 优化扩展表作为**模型推广与延伸应用**小节；
4. 第七节"机器学习定位"段落作为模型推广部分的收尾；
5. 引用文件：`p1~p3/论文写作指南.md`、`p4/论文写作指南_evsi版.md`（各题细节）、`论文写作总纲.md`（全卷口径）。
