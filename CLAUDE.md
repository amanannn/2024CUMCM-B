# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 项目背景

2024年高教社杯全国大学生数学建模竞赛 B 题《生产过程中的决策问题》。
本目录包含题目原文（`B题.pdf`）、可读版本（`B题_Claude_Friendly.md`）以及解题过程中产生的全部代码、数据和图表。

团队角色分工：编程手兼队长（用户本人），另有建模手（负责建模思路）和论文手（负责撰写论文）。编程手负责全部代码实现、数据记录与画图，并与建模手共同确认方案。

## 运行环境

- **Python**：使用 conda 环境 `mcm`，解释器路径 `C:/Users/gu/miniconda3/envs/mcm/python.exe`（Python 3.11.14，含 numpy / scipy / matplotlib / pandas）。不要用系统默认 python（无科学计算库）。
- **MATLAB**：路径 `D:/matlab/bin/matlab.exe`，用于 3D 示意类图；数据分析类图一律用 Python（matplotlib）绘制。
  - 运行方式（注意 cwd 会偏移，须显式 cd 到项目根并在脚本内用相对路径）：
    `"D:/matlab/bin/matlab.exe" -batch "cd('D:/虚拟C盘/数模/2024B题'); run('p3/p3_flow_3d.m')"`
  - 脚本内输出目录需先 mkdir（exportgraphics 不自动建目录）；标签用英文避免字体问题。
- 脚本一律通过 `C:/Users/gu/miniconda3/envs/mcm/python.exe <脚本>` 运行，禁止使用 `python` 裸命令。

## 工作流程（用户强制规定，必须遵守）

1. **每道题开始前**：先与用户讨论并确定最佳解题方案，讨论达成一致后才允许写代码。
2. **求解**：每道题用 Python（或必要时 MATLAB）写成**独立文件**求解，得出可行答案并**记录数据**（保存为 csv/json 等数据文件）。
3. **画图**：根据记录的数据画图。数据分析类图 → Python；3D 示意类图 → MATLAB。
4. **论文写作指南**：每一道题完成（求解+画图）后，结合建模手的意见、用户的意见和求解结果，为论文手生成一份论文写作指南。
5. **全部问题解决后**：与用户商讨整体优化方案，再决定是否迭代。

## 目录结构约定

- 每个问题一个子目录（如 `p1/`、`p2/`），内含求解脚本、生成的数据文件和图表。
- 图表统一输出为 PNG，保存于对应问题目录下。
- 论文写作指南输出为 Markdown 文件（已完成四份：p1~p4/论文写作指南.md + 根目录 论文写作总纲.md）。

## 经验教训（重要，编程手指出）

- **问题4 方法缺陷（教训）**：题目说"次品率是通过抽样检测方法得到的，请重新完成问题2/3"——侧重点是**方法**。正确做法是把"抽样"建进决策链：n 是决策变量、观测 k 随机、后验 Beta(a+k, b+n−k) 是决策依据、目标 V(n)=ΣₖP(k)·maxₓE_{p|k}[π(p,x)]−n·d、指标含 EVSI（抽样信息价值）。我最初的实现只做了"CI 端点重解"（参数敏感性），被编程手指出是"代数字不是方法"。**建模前必须先问：题目侧重点是方法还是结果。**
- 2026-08-12 已完成：问题4 已按 EVSI 框架重做（p4/p4_evsi.py + p4_evsi_sensitivity.py + 论文写作指南_evsi版.md），CI 端点重解降级为辅助。关键修正：抽样成本是"每批一次"摊入批量 B（B=100/1000/10000 敏感性），批量决定抽样值不值；n_prior=30 先验、d=0.5/2/5 检测费敏感性。
- 待讨论：纳什均衡在问题1（供应商-企业博弈，信息不对称/信号博弈）的建模。

## 当前进度（2026-08-12 已全部完成）

- 问题1~4 均已求解+画图+写作指南；优化项（论文总纲、SPRT扩展、样本量-稳健性分析、公开解答核对）已完成。
- 关键口径（与建模手/编程手确认）：P1 用"判定可靠度"读法（n=29/n=22）；P2/P3 用"每服务一个客户"口径（与题目调换要求一致），拆解/报废两分支统一；P4 用 n=22 + Clopper-Pearson 90% CI + 蒙特卡洛误差传播（M=5000/300）。
- 权威题目数据以 `B题_Claude_Friendly.md` 现行版本为准（表2：半成品1/2/3 检测均4、拆解均6；成品检测6、拆解10、售价200、调换40）。

## 题目结构

- 问题1：抽样检测方案设计（样本量最小化，两类信度要求：95% 拒收 / 90% 接收）
- 问题2：两零配件+成品生产决策（检测、拆解、调换各环节的 0-1 决策，共 6 种情形）
- 问题3：m 道工序、n 个零配件（2 道工序 8 个零配件），推广问题2 的决策
- 问题4：次品率本身由抽样检测获得，结合问题1 的方法重做问题2、3（考虑抽样误差的不确定性传播）
